= content_for :page_title do
   = Spree.t(:blog_entries)
     %i.icon-arrow-right= "#{Spree.t(:editing)} #{blog_entry.title}"
= content_for :page_actions do
  %li= button_link_to Spree.t(:back), spree.admin_blog_entries_url, icon: 'icon-arrow-left'
= render "spree/shared/error_messages", target: @blog_entry
= form_for [:admin, @blog_entry] do |f|
  = render partial: f
  = render partial: 'spree/admin/shared/edit_resource_links'
